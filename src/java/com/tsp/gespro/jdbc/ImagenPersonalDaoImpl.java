/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.tsp.gespro.jdbc;

import com.tsp.gespro.dao.*;
import com.tsp.gespro.factory.*;
import com.tsp.gespro.dto.*;
import com.tsp.gespro.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ImagenPersonalDaoImpl extends AbstractDAO implements ImagenPersonalDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_IMAGEN_PERSONAL, ID_EMPRESA, NOMBRE_IMAGEN, UTILIZAR FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_IMAGEN_PERSONAL, ID_EMPRESA, NOMBRE_IMAGEN, UTILIZAR ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_IMAGEN_PERSONAL = ?, ID_EMPRESA = ?, NOMBRE_IMAGEN = ?, UTILIZAR = ? WHERE ID_IMAGEN_PERSONAL = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_IMAGEN_PERSONAL = ?";

	/** 
	 * Index of column ID_IMAGEN_PERSONAL
	 */
	protected static final int COLUMN_ID_IMAGEN_PERSONAL = 1;

	/** 
	 * Index of column ID_EMPRESA
	 */
	protected static final int COLUMN_ID_EMPRESA = 2;

	/** 
	 * Index of column NOMBRE_IMAGEN
	 */
	protected static final int COLUMN_NOMBRE_IMAGEN = 3;

	/** 
	 * Index of column UTILIZAR
	 */
	protected static final int COLUMN_UTILIZAR = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_IMAGEN_PERSONAL
	 */
	protected static final int PK_COLUMN_ID_IMAGEN_PERSONAL = 1;

	/** 
	 * Inserts a new row in the imagen_personal table.
	 */
	public ImagenPersonalPk insert(ImagenPersonal dto) throws ImagenPersonalDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdImagenPersonalModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_IMAGEN_PERSONAL" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_EMPRESA" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isNombreImagenModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "NOMBRE_IMAGEN" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isUtilizarModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "UTILIZAR" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString(), Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.isIdImagenPersonalModified()) {
				stmt.setInt( index++, dto.getIdImagenPersonal() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isNombreImagenModified()) {
				stmt.setString( index++, dto.getNombreImagen() );
			}
		
			if (dto.isUtilizarModified()) {
				if (dto.isUtilizarNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setShort( index++, dto.getUtilizar() );
				}
		
			}
		
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdImagenPersonal( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImagenPersonalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the imagen_personal table.
	 */
	public void update(ImagenPersonalPk pk, ImagenPersonal dto) throws ImagenPersonalDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdImagenPersonalModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_IMAGEN_PERSONAL=?" );
				modified=true;
			}
		
			if (dto.isIdEmpresaModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_EMPRESA=?" );
				modified=true;
			}
		
			if (dto.isNombreImagenModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "NOMBRE_IMAGEN=?" );
				modified=true;
			}
		
			if (dto.isUtilizarModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "UTILIZAR=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_IMAGEN_PERSONAL=?" );
			System.out.println( "Executing " + sql.toString() + " with values: " + dto );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdImagenPersonalModified()) {
				stmt.setInt( index++, dto.getIdImagenPersonal() );
			}
		
			if (dto.isIdEmpresaModified()) {
				stmt.setInt( index++, dto.getIdEmpresa() );
			}
		
			if (dto.isNombreImagenModified()) {
				stmt.setString( index++, dto.getNombreImagen() );
			}
		
			if (dto.isUtilizarModified()) {
				if (dto.isUtilizarNull()) {
					stmt.setNull( index++, java.sql.Types.INTEGER );
				} else {
					stmt.setShort( index++, dto.getUtilizar() );
				}
		
			}
		
			stmt.setInt( index++, pk.getIdImagenPersonal() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImagenPersonalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the imagen_personal table.
	 */
	public void delete(ImagenPersonalPk pk) throws ImagenPersonalDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdImagenPersonal() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImagenPersonalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the imagen_personal table that matches the specified primary-key value.
	 */
	public ImagenPersonal findByPrimaryKey(ImagenPersonalPk pk) throws ImagenPersonalDaoException
	{
		return findByPrimaryKey( pk.getIdImagenPersonal() );
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the criteria 'ID_IMAGEN_PERSONAL = :idImagenPersonal'.
	 */
	public ImagenPersonal findByPrimaryKey(int idImagenPersonal) throws ImagenPersonalDaoException
	{
		ImagenPersonal ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_IMAGEN_PERSONAL = ?", new Object[] {  new Integer(idImagenPersonal) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the criteria ''.
	 */
	public ImagenPersonal[] findAll() throws ImagenPersonalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_IMAGEN_PERSONAL", null );
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the criteria 'ID_EMPRESA = :idEmpresa'.
	 */
	public ImagenPersonal[] findByEmpresa(int idEmpresa) throws ImagenPersonalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_EMPRESA = ?", new Object[] {  new Integer(idEmpresa) } );
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the criteria 'ID_IMAGEN_PERSONAL = :idImagenPersonal'.
	 */
	public ImagenPersonal[] findWhereIdImagenPersonalEquals(int idImagenPersonal) throws ImagenPersonalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_IMAGEN_PERSONAL = ? ORDER BY ID_IMAGEN_PERSONAL", new Object[] {  new Integer(idImagenPersonal) } );
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the criteria 'ID_EMPRESA = :idEmpresa'.
	 */
	public ImagenPersonal[] findWhereIdEmpresaEquals(int idEmpresa) throws ImagenPersonalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_EMPRESA = ? ORDER BY ID_EMPRESA", new Object[] {  new Integer(idEmpresa) } );
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the criteria 'NOMBRE_IMAGEN = :nombreImagen'.
	 */
	public ImagenPersonal[] findWhereNombreImagenEquals(String nombreImagen) throws ImagenPersonalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NOMBRE_IMAGEN = ? ORDER BY NOMBRE_IMAGEN", new Object[] { nombreImagen } );
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the criteria 'UTILIZAR = :utilizar'.
	 */
	public ImagenPersonal[] findWhereUtilizarEquals(short utilizar) throws ImagenPersonalDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE UTILIZAR = ? ORDER BY UTILIZAR", new Object[] {  new Short(utilizar) } );
	}

	/**
	 * Method 'ImagenPersonalDaoImpl'
	 * 
	 */
	public ImagenPersonalDaoImpl()
	{
	}

	/**
	 * Method 'ImagenPersonalDaoImpl'
	 * 
	 * @param userConn
	 */
	public ImagenPersonalDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "imagen_personal";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ImagenPersonal fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ImagenPersonal dto = new ImagenPersonal();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ImagenPersonal[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ImagenPersonal dto = new ImagenPersonal();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ImagenPersonal ret[] = new ImagenPersonal[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ImagenPersonal dto, ResultSet rs) throws SQLException
	{
		dto.setIdImagenPersonal( rs.getInt( COLUMN_ID_IMAGEN_PERSONAL ) );
		dto.setIdEmpresa( rs.getInt( COLUMN_ID_EMPRESA ) );
		dto.setNombreImagen( rs.getString( COLUMN_NOMBRE_IMAGEN ) );
		dto.setUtilizar( rs.getShort( COLUMN_UTILIZAR ) );
		if (rs.wasNull()) {
			dto.setUtilizarNull( true );
		}
		
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ImagenPersonal dto)
	{
		dto.setIdImagenPersonalModified( false );
		dto.setIdEmpresaModified( false );
		dto.setNombreImagenModified( false );
		dto.setUtilizarModified( false );
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the specified arbitrary SQL statement
	 */
	public ImagenPersonal[] findByDynamicSelect(String sql, Object[] sqlParams) throws ImagenPersonalDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImagenPersonalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the imagen_personal table that match the specified arbitrary SQL statement
	 */
	public ImagenPersonal[] findByDynamicWhere(String sql, Object[] sqlParams) throws ImagenPersonalDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImagenPersonalDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
